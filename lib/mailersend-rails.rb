# frozen_string_literal: true

# The gem is `mailersend-rails`, so Bundler's auto-require looks for this file. Everything
# lives under the underscored name, which is the one that matches the constant.
#
# The dash is not a style choice: `mailersend_rails` on RubyGems is an unrelated gem by
# another author, and RubyGems treats the two spellings as different names.
require "mailersend_rails"
